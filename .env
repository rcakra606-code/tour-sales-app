# ==========================================================
# 🌍 SERVER CONFIGURATION
# ==========================================================

# Port server backend
PORT=5000

# Mode environment (production | development)
NODE_ENV=production

# URL frontend yang mengakses backend
# Jika di Render gunakan alamat domain deploy kamu
FRONTEND_URL=https://tour-sales-app.onrender.com

# ==========================================================
# 🔐 AUTHENTICATION (JWT)
# ==========================================================

# Secret key untuk generate token JWT (ubah di production)
JWT_SECRET=supersecretkey123

# Lama waktu hidup token utama (15 menit)
JWT_EXPIRES_IN=15m

# Lama waktu refresh token (7 hari)
JWT_REFRESH_EXPIRES_IN=7d

# ==========================================================
# 💾 DATABASE CONFIGURATION
# ==========================================================

# Path file database SQLite (lokasi default)
DB_PATH=./data/database.sqlite

# ==========================================================
# 💾 BACKUP CONFIGURATION
# ==========================================================

# Folder tempat menyimpan backup
BACKUP_DIR=./backups

# Jadwal cron auto-backup (setiap jam 3 pagi)
# Format: menit jam hari-bulan bulan hari-minggu
CRON_BACKUP_SCHEDULE=0 3 * * *

# ==========================================================
# ✉️ EMAIL CONFIGURATION (opsional, jika ingin notifikasi)
# ==========================================================

# Gunakan Gmail SMTP atau kosongkan jika belum digunakan
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASS=

# Nama pengirim email (opsional)
MAIL_FROM_NAME=Travel Dashboard System
MAIL_FROM_ADDRESS=

# ==========================================================
# 🧠 SECURITY SETTINGS
# ==========================================================

# CSP fix agar Tailwind, Chart.js, SweetAlert berfungsi
# Tidak perlu ubah kecuali ingin menambah CDN
CSP_SCRIPT_SRC='self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdn.tailwindcss.com https://unpkg.com https://cdn.jsdelivr.net/npm/sweetalert2@11
CSP_STYLE_SRC='self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com
CSP_FONT_SRC='self' https://fonts.gstatic.com
CSP_IMG_SRC='self' data: https://cdn.jsdelivr.net

# ==========================================================
# 📦 FILE UPLOAD (opsional)
# ==========================================================

# Folder penyimpanan upload file
UPLOAD_DIR=./uploads

# Maksimum ukuran file (dalam MB)
MAX_FILE_SIZE_MB=10
